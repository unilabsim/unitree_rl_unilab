# @package _global_
defaults:
  - /task/unitree_go2w_joystick_rough/mujoco
  - _self_

training:
  task_name: UnitreeGo2WJoystickRough
  sim_backend: motrix

env:
  render_offset_mode: zero
  scene:
    model_file: src/unitree_rl_unilab/assets/robots/go2w/go2w.xml
  events:
    push_robot: null

reward:
  hip_pos:
    weight: -0.5
